"""
data.py
Handles loading and preprocessing of asset price data.
"""

import pandas as pd


def load_data(asset1_path: str, asset2_path: str) -> pd.DataFrame:
    """
    Loads price data for two assets and aligns them by date.
    Returns a DataFrame with columns: ['date', 'asset1', 'asset2']
    """
    # ...implementation...
    pass
